import {db} from "./fbconfig";
import {collection, addDoc, getDocs, deleteDocs, serverTimestamp, query, orderBy, doc, updateDoc} from 'firebase/firestore';


async function addTodo() {
}

export default function Home() {
  return (
    <main className="pt-20">
    </main>
  );
}
